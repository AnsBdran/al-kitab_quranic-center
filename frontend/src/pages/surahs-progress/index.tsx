import { Container } from '@mantine/core';
import { ComingSoon } from '../../components';
const SurahsProgress = () => {
  return (
    <Container>
      <ComingSoon />
    </Container>
  );
};

export default SurahsProgress;
